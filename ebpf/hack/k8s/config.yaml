# K8s deployment config
# For local dev: IP is the Mac host gateway from inside the Lima VM.
# For production: use "qubit-core.default.svc.cluster.local"
qubit_core:
  host: "IP"
  grpc_port: 50051

perf_array_name: "NETWORK_EVENTS"

# Path inside the container
ebpf_path: "/app/ebpf-bytecode"
